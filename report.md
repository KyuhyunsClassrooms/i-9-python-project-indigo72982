# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제: 터미널 포켓몬 2D 게임

이 프로그램을 만든 이유: 터미널로 초저사양의 기기도 할 수 있는 재밌는 게임을 만들어보고 싶다.


---

## 2. 문제 분해 및 모델링

### 입력
사용자 조작 입력: 키보드 실시간 입력(w, a, s, d로 이동, 숫자키 1~4로 전투 및 가방 메뉴 선택).
초기 시스템 데이터: 포켓몬의 능력치 정보가 담긴 2차원 리스트 데이터.
확률 데이터: 야생 포켓몬 조우 확률(15%) 및 포획 확률 계산을 위한 난수(Random) 값.

### 처리
좌표 연산: 플레이어가 이동할 때 좌표(p_x, p_y)를 갱신하고, 값이 0이나 31이 되지 않도록 제한하여 테두리 벽(#)을 넘어가지 못하게 막음.
전투 수치 계산: 아군 공격 시 상대 HP 차감, 적 공격 시 아군 HP 차감, 상처약 사용 시 최대 HP의 50%를 더해주는 연산 처리.
실시간 포획 확률 판정: 상대의 현재 HP와 최대 HP의 비율을 계산하여 체력이 낮을수록 포획 성공률이 높아지는 알고리즘 적용.

### 출력
2D 격자 지도: 화면을 갱신(clear)하며 실시간으로 그려지는 32x32 크기의 텍스트 맵.
전투 인터페이스: 야생 포켓몬 등장 메시지, 아군과 적군의 실시간 HP 잔량 표시, 소지한 포켓몬 파티 목록 정보창.

---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름: pokemon_db , my_party

한 행의 의미: 게임에 존재하는 개별 포켓몬 캐릭터 1마리의 이름과 고유 고정 능력치 세트.

각 열의 의미:

| 열 번호 | 의미 | 실제 예시 |
|---:|---|---|
| 0번 열 | 포켓몬의 이름 | "피카츄" |
| 1번 열 | 포켓몬이 가질 수 있는 최대 체력 | 50 |
| 2번 열 | 포켓몬의 현재 남아있는 체력 | 50 |
| 3번 열 | 포켓몬이 적에게 입히는 공격력 | 15 |

이 2차원 리스트를 사용한 이유:
포켓몬이라는 하나의 객체가 가지는 여러 가지 성질(이름, 피통, 공격력)을 문자열과 정수형태로 묶어서 관리해야 효율적이기 때문이다. 여러 개의 리스트를 따로 만드는 것보다, 2차원 리스트를 사용하면 my_party[0][0](이름), my_party[0][1](최대HP)처럼 인덱스 번호 하나로 특정 포켓몬의 모든 정보를 한 번에 접근하고 수정할 수 있어 데이터 관리가 매우 편리하다.

---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| get_char() | 엔터키 없이 누르는 즉시 키보드 값을 읽어옴 | x | 사용자가 입력한 문자 1개 |
| init_game() | 플레이어 위치, 맵 크기, 보유 파티 등 시작 데이터 설정 | x | 없음 (전역변수 초기화) |
| draw_map() | 터미널 창을 청소하고 32x32 맵과 플레이어 위치를 출력 | x | 터미널 화면에 텍스트 지도 출력 |
| battle() | 턴제 전투, 도구 사용, 포켓몬 교체, 게임오버 판정 등 처리 | x | 전투 인터페이스 및 결과 메시지 출력 |
| main() | 전체 게임의 핵심 루프를 돌며 이동 입력 및 전투 발생 제어 | x | 없음 (게임 전체 제어) |

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름: battle()

설명: 야생 포켓몬을 만났을 때 실행되는 독자적인 무한루프 함수로, 게임의 핵심 재미 요소들을 모두 담고 있다. 사용자가 '싸우기'를 선택하면 수치 연산 후 적의 HP를 깎고, '가방'을 선택하면 상대 체력에 반비례하는 확률 공식을 적용해 몬스터볼 포획 시스템을 연산한다. 또한 내 포켓몬이 쓰러졌을 때 파티 리스트(my_party)를 순회하며 살아있는 포켓몬이 있는지 체크하고 자동으로 다음 포켓몬을 출전시키며, 전멸 시 init_game()을 호출해 게임을 처음부터 재시작하는 중추적인 예외/흐름 제어 역할을 수행한다.


---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

어떤 조건을 검사했는가?
: 플레이어가 이동한 새로운 좌표가 맵의 경계(0보다 크고 31보다 작은 범위) 안에 들어오는지 체크하여 벽 통과를 방지했다. 가방 탭에서 몬스터볼을 던졌을 때, 시스템이 뽑은 랜덤 숫자(1~100)가 내 포획 확률(prob)보다 작거나 같은지 검사하여 포획 성공 여부를 판정했다. 전투 도중 아군 포켓몬의 HP가 0 이하가 되었는지를 수시로 검사하여 기절 및 자동 교체, 전멸 판정을 내렸다.

### 반복문 사용 부분

무엇을 반복 처리했는가?
: main() 함수에서 while True 문을 사용하여 사용자가 q를 눌러 종료하기 전까지 맵을 그리고 키 입력을 받는 행동을 무한 반복한다. draw_map() 함수에서 이중 for 문을 사용하여 세로 32줄, 가로 32칸을 돌며 총 1,024번 격자를 탐색해 지도 모양을 화면에 반복해서 그려낸다. 포켓몬 파티 교체 메뉴를 열었을 때 for i, p in enumerate(my_party) 문을 사용하여 내가 소지한 포켓몬들의 이름과 체력 정보를 리스트 순서대로 뽑아 화면에 목록화한다.

### 조건문과 반복문이 함께 작동한 부분
: battle() 함수의 포켓몬 기절 처리 파트이다. while 문으로 전투가 반복되는 도중, 아군 포켓몬의 HP가 0 이하가 되면 if 조건문이 실행된다. 그 내부에서 다시 for 반복문이 작동하여 내 포켓몬 파티 리스트 전체를 처음부터 순회하면서, 살아있는 포켓몬이 있는지 if p[1] > 0 조건문으로 검사한다. 살아있는 포켓몬을 찾으면 인덱스를 교체하고 반복문을 break로 탈출하며, 한 마리도 찾지 못하면(if not alive) 게임 오버 조건문이 발동하여 게임이 리셋된다.

---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

```
# # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # 
# . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . # 
# . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . # 
# . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . # 
# . . . . . . . . . . . . . P . . . . . . . . . . . . . . . . # 
# . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . # 
... (중략) ...
# # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # # 

[WASD] 이동 | [Q] 종료

⚠️ 야생의 포켓몬이 나타났다!

=== 전투 화면 ===
상대: 파이리 (HP: 60/60)
나의 포켓몬: 피카츄 (HP: 50/50)
-----------------
1. 싸우기 | 2. 가방 (몬스터볼/상처약) | 3. 포켓몬 교체 | 4. 도망치기
```

결과 분석:
프로그램이 실행되면 32x32 크기의 월드 맵 한가운데에 플레이어인 P가 정상적으로 표시된다. 키보드로 w, a, s, d를 누르면 P가 빈 공간(.)을 따라 부드럽게 실시간 이동하는 것을 볼 수 있으며, 가장자리 벽(#)에 부딪히면 더 이상 밖으로 나가지 않는다. 풀숲을 돌아다니듯 걷다 보면 확률적으로 화면이 지워지면서 야생 포켓몬 조우 메시지가 뜨고, 공격 및 아이템 사용 기능이 포함된 턴제 텍스트 전투 화면으로 정상 진입함을 확인했다.

---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력: 플레이어의 위치가 맨 왼쪽 구석 (1, 1)일 때 왼쪽 이동 키 a 입력, 그리고 전투 메뉴 선택창에서 1~4가 아닌 알파벳 z 입력.

예상 결과: 벽 밖으로 탈출하면 안 되므로 좌표가 고정되어야 하며, 메뉴창에서 잘못된 키를 입력했을 때는 에러로 튕기지 않고 무시된 뒤 메뉴가 다시 떠야 함.

실제 결과: 범위 제한 조건문 덕분에 좌표가 변경되지 않아 벽을 뚫지 못했고, 전투 메뉴에서 z를 누르자 else: continue 구문이 동작하여 예외 오류 없이 전투 화면이 깔끔하게 유지됨.


### 테스트 2: 예외 또는 경계 사례

입력: 플레이어의 위치가 맨 왼쪽 구석 (1, 1)일 때 왼쪽 이동 키 a 입력, 그리고 전투 메뉴 선택창에서 1~4가 아닌 알파벳 z 입력.

예상 결과: 벽 밖으로 탈출하면 안 되므로 좌표가 고정되어야 하며, 메뉴창에서 잘못된 키를 입력했을 때는 에러로 튕기지 않고 무시된 뒤 메뉴가 다시 떠야 함.

실제 결과: 범위 제한 조건문 덕분에 좌표가 변경되지 않아 벽을 뚫지 못했고, 전투 메뉴에서 z를 누르자 else: continue 구문이 동작하여 예외 오류 없이 전투 화면이 깔끔하게 유지됨.


### 발견한 오류와 수정한 방법
오류 내용: 처음 코드를 실행했을 때 main() 함수 부분에서 SyntaxError: name 'p_x' is used prior to global declaration 에러가 발생하며 프로그램이 강제 종료되었다.

수정 방법: 파이썬 문법 규칙상 함수 안에서 전역 변수의 값을 변경하려면 변수를 사용하기 전 최상단에 global 선언을 해야 한다. 기존 코드에서는 next_x = p_x로 전역 변수 값을 먼저 조회한 뒤 아래쪽에서 global p_x를 선언하여 발생한 순서 오류였다. main() 함수가 시작하자마자 첫 줄에 global p_x, p_y 코드가 위치하도록 선언문의 순서를 위로 올바르게 재배치하여 오류를 완벽히 해결했다.

---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분: 전체적인 메커니즘 설계 및 코딩


Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분: 텍스트 가독성


최종 코드에서 내가 직접 판단하거나 수정한 부분: 텍스트 가독성


---

## 9. 한계와 개선 방안

현재 프로그램의 한계: 
텍스트 터미널 기반 게임 특성상 플레이어가 한 칸 움직일 때마다 전체 화면을 지우고 다시 그리는 방식(clear)을 사용하여, 컴퓨터 성능이나 터미널 환경에 따라 화면이 약간 깜빡거리는 현상이 존재한다.
무한 몬스터볼과 상처약이 제공되어 가방 인벤토리의 한계가 없고, 포켓몬들의 고유 스킬이 없이 단순히 일반 공격력 수치 하나로만 대미지를 주고받아 단조로운 감이 있다.


다시 개선한다면 추가하고 싶은 기능: 
몸통박치기, 백만볼트 등 포켓몬마다 속성과 연출 기술을 4개씩 고를 수 있는 '기술 선택 리스트' 시스템을 추가하고 싶다.
32x32 공간 안에 단순히 빈 땅만 있는 것이 아니라, 실제 포켓몬 게임처럼 '풀숲' 구역을 2차원 리스트 좌표로 따로 지정하여 풀숲 구역 위를 걸어 다닐 때만 야생 포켓몬이 등장하도록 고도화하고 싶다.


---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
4. 조건문과 반복문은 어디에서 사용되었는가?
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
